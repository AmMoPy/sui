import{elements}from"./elements.min.js";import{state,saveState}from"./state.min.js";import{getDB,debugDB}from"./database.min.js";import{COMPRESSION_THRESHOLD,ALLOWED_AUDIO_TYPES,FILE_SIZE_LIMITS,CONVERSION_PRESETS,CONVERSION_OPTIONS,AUDIO_SIGNATURES}from"./constants.min.js";import{showStatusMessage,renderPlayList,speakText}from"./ui.min.js";import{sortItems}from"./sorting.min.js";import{loadItem}from"./playback.min.js";import{FFMPEG_BASE,FFMPEG_C_V}from"../versions.js";import{getFFmpegPath}from"./vgp.js";async function handleFileUpload(e){const files=Array.from(e.target.files);if(!files.length)return;e.target.value="";const totalSize=files.reduce(((total,file)=>total+file.size),0);const totalFiles=files.length;if(totalSize>100*1024*1024||totalFiles>50){const proceed=confirm(`You're uploading ${totalFiles} files (${formatFileSize(totalSize)}). `+`Large uploads may cause performance issues. Continue?`);if(!proceed)return}const BATCH_SIZE=5;for(let i=0;i<files.length;i+=BATCH_SIZE){const batch=files.slice(i,i+BATCH_SIZE);await Promise.all(batch.map((async file=>{try{await processFile(file)}catch(error){console.error(`Processing failed for ${file.name}:`,error);uploadManager.updateProgress(file,0,`Error: ${error.message}`);showStatusMessage(`Failed to process ${file.name}: ${error.message}`,"error")}})));if(i+BATCH_SIZE<files.length){await new Promise((resolve=>setTimeout(resolve,300)))}}await loadItems(false)}async function loadItems(autoplay=false){if(state.currentAudioUrl){URL.revokeObjectURL(state.currentAudioUrl);state.currentAudioUrl=null}try{const database=await getDB();const tx=database.transaction("items","readonly");const store=tx.objectStore("items");const allItems=await new Promise(((resolve,reject)=>{const req=store.getAll();req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)}));const sortedItems=sortItems(allItems,state.sortPreferences);state.items=sortedItems;const hasDefaultItems=allItems.some((item=>item.type==="default"));if(!hasDefaultItems&&!state.defaultsDeleted&&!state.defaultsInitialized){try{const defaultItems=[{name:"1",path:"assets/main/better_day.mp3",type:"default"},{name:"2",path:"assets/main/care.mp3",type:"default"}];const addTx=database.transaction("items","readwrite");const addStore=addTx.objectStore("items");for(const item of defaultItems){addStore.add(item)}await new Promise(((resolve,reject)=>{addTx.oncomplete=()=>{state.defaultsInitialized=true;saveState();resolve()};addTx.onerror=()=>reject(addTx.error)}));const newTx=database.transaction("items","readonly");const newStore=newTx.objectStore("items");const updatedItems=await new Promise(((resolve,reject)=>{const req=newStore.getAll();req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)}));state.items=updatedItems}catch(error){console.error("Failed to add default items:",error)}}else if(hasDefaultItems){state.defaultsInitialized=true;saveState()}renderPlayList();if(state.items.length>0){const prevIndex=state.currentIndex;if(state.currentIndex>=state.items.length){state.currentIndex=state.items.length-1}if(autoplay){loadItem(state.currentIndex)}else{const item=state.items[state.currentIndex];if(item){if(item.path){state.audio.src=item.path}else if(item.data){const blob=new Blob([item.data],{type:"audio/mp3"});state.currentAudioUrl=URL.createObjectURL(blob);state.audio.src=state.currentAudioUrl}}}}else{state.audio.pause();state.audio.removeAttribute("src");state.audio.load();elements.playPauseBtn.textContent="▶";console.log("No items in playlist");speakText("Empty")}}catch(error){console.error("Error loading playlist items:",error);showStatusMessage("Failed to load playlist","error")}}class UploadManager{constructor(){this.progressBars=new Map;this.activeConversions=new Map;this.conversionResults=new Map;this.conversionListeners=new Map;this.hasFFmpeg=false;this.ffmpegLoading=false;this.ffmpegLoadPromise=null;this.conversionQueue=[];this.ffmpegInstances=[];this.maxParallelConversions=3;this.currentlyProcessing=0;this.initFFmpegSystem()}async initFFmpegSystem(){if(this.ffmpegLoading){return this.ffmpegLoadPromise}this.ffmpegLoading=true;this.ffmpegLoadPromise=new Promise((async resolve=>{try{const{FFmpeg:FFmpeg}=window.FFmpegWASM;const primaryInstance=new FFmpeg;primaryInstance.on("progress",(({progress:progress,time:time})=>{if(progress<=1){console.log(`FFmpeg progress: ${Math.floor(progress*100)}%`);const currentFile=this.currentlyProcessingFile?this.currentlyProcessingFile:null;this.showFFmpegLoadingStatus(progress,currentFile)}else{console.log(`FFmpeg processing: stream reading phase`);this.showFFmpegLoadingStatus(-1,this.currentlyProcessingFile)}}));primaryInstance.on("log",(({message:message})=>{console.log(`FFmpeg log: ${message}`)}));await primaryInstance.load({coreURL:getFFmpegPath(FFMPEG_BASE,"ffmpeg-core.js",FFMPEG_C_V),wasmURL:getFFmpegPath(FFMPEG_BASE,"ffmpeg-core.wasm",FFMPEG_C_V)});this.ffmpegInstances.push({instance:primaryInstance,busy:false,id:"primary"});const additionalInstances=this.maxParallelConversions-1;for(let i=0;i<additionalInstances;i++){try{const instance=new FFmpeg;const instanceId=`worker-${i+1}`;instance.on("progress",(({progress:progress})=>{console.log(`FFmpeg ${instanceId} progress: ${Math.floor(progress*100)}%`);const entry=this.ffmpegInstances.find((e=>e.id===instanceId));this.showFFmpegLoadingStatus(progress,entry?.currentFile,instanceId)}));await instance.load({coreURL:getFFmpegPath(FFMPEG_BASE,"ffmpeg-core.js",FFMPEG_C_V),wasmURL:getFFmpegPath(FFMPEG_BASE,"ffmpeg-core.wasm",FFMPEG_C_V)});this.ffmpegInstances.push({instance:instance,busy:false,id:instanceId,currentFile:null});console.log(`FFmpeg ${instanceId} ready`)}catch(err){console.warn(`Failed to initialize FFmpeg ${instanceId}:`,err)}}console.log(`FFmpeg system ready with ${this.ffmpegInstances.length} instances`);this.hasFFmpeg=true;this.processQueue();resolve(true)}catch(err){console.error("FFmpeg initialization failed:",err);this.hasFFmpeg=false;resolve(false)}finally{this.ffmpegLoading=false}}));return this.ffmpegLoadPromise}getAvailableFFmpegInstance(){for(const ffmpegEntry of this.ffmpegInstances){if(!ffmpegEntry.busy&&!ffmpegEntry.failed){ffmpegEntry.busy=true;return ffmpegEntry}}return null}releaseFFmpegInstance(instanceId){const instanceEntry=this.ffmpegInstances.find((entry=>entry.id===instanceId));if(instanceEntry){instanceEntry.busy=false;console.log(`Released FFmpeg instance: ${instanceId}`);this.processQueue()}}showFFmpegLoadingStatus(progress,fileName=null,instanceId=""){if(!fileName)return;const uploadItem=this.progressBars.get(fileName);if(!uploadItem)return;let ffmpegSection=uploadItem.querySelector(".ffmpeg-progress");if(!ffmpegSection){ffmpegSection=document.createElement("div");ffmpegSection.className="ffmpeg-progress";const progressContainer=uploadItem.querySelector(".progress-container");if(progressContainer){progressContainer.appendChild(ffmpegSection)}}ffmpegSection.innerHTML=`\n      <div class="ffmpeg-instance-info">\n        <span class="instance-id">${instanceId}</span>\n        <span class="progress-label">${progress>=0?`${Math.floor(progress*100)}%`:"Analyzing..."}</span>\n      </div>\n    `;const progressBar=uploadItem.querySelector("progress");const statusEl=uploadItem.querySelector(".status");if(progressBar){progressBar.value=progress>=0?Math.max(20,progress*100):20}if(statusEl){statusEl.textContent=progress>=0?`Converting: ${Math.floor(progress*100)}%`:"Analyzing audio..."}uploadItem.dataset.instanceId=instanceId}async queueAndWaitForConversion(file,validation,options){return new Promise(((resolve,reject)=>{let timeout;const conversionListener=(result,error)=>{clearTimeout(timeout);this.removeConversionListener(file.name,conversionListener);if(error){console.error(`Conversion error for ${file.name}:`,error);reject(error)}else{resolve(result||file)}};timeout=setTimeout((()=>{if(this.activeConversions.has(file.name)){console.warn(`Timeout but conversion still active for ${file.name}`);return}this.removeConversionListener(file.name,conversionListener);this.ensureCleanup(file.name);console.warn(`Conversion timeout for ${file.name}`);reject(new Error("Conversion timed out"))}),3e4);this.addConversionListener(file.name,conversionListener);this.addToQueue(file,validation,options)}))}ensureCleanup(fileName){const normalizedFileName=fileName.replace(/[^a-zA-Z0-9]/g,"-");const ffmpegElements=document.querySelectorAll(`[id*="-${normalizedFileName}"]`);ffmpegElements.forEach((el=>el.remove()));document.querySelectorAll(`.ffmpeg-status-item[data-file*="${normalizedFileName}"]`).forEach((el=>el.remove()));const progressEl=this.progressBars.get(fileName);if(progressEl){progressEl.remove();this.progressBars.delete(fileName)}console.log(`Cleaned up UI elements for ${fileName}`)}addToQueue(file,validation,options=null){const queueItem={id:`task-${Date.now()}-${Math.random().toString(36).substr(2,9)}`,file:file,validation:validation,options:options,status:"queued",active:true,priority:1};this.conversionQueue.push(queueItem);console.log(`Added ${file.name} to conversion queue. Queue size: ${this.conversionQueue.length}`);this.updateProgress(file,0,"Queued for processing...");this.processQueue();return queueItem}async processQueue(){if(!this.hasFFmpeg){console.log("FFmpeg not ready, can't process queue yet");return}await this.verifyFFmpegInstances();const pendingItems=this.conversionQueue.filter((item=>item.active&&item.status==="queued"));if(pendingItems.length===0){console.log("No pending items in conversion queue");return}console.log(`Processing conversion queue. Pending items: ${pendingItems.length}`);for(const queueItem of pendingItems){if(this.currentlyProcessing>=this.maxParallelConversions){console.log(`Max parallel conversions (${this.maxParallelConversions}) reached. Waiting...`);break}const ffmpegEntry=this.getAvailableFFmpegInstance();if(!ffmpegEntry){console.log("No available FFmpeg instances");break}queueItem.status="processing";this.currentlyProcessing++;this.updateProgress(queueItem.file,15,"Starting conversion...");console.log(`Starting conversion of ${queueItem.file.name} with instance ${ffmpegEntry.id}`);this.processConversion(queueItem,ffmpegEntry).catch((error=>{console.error(`Error processing ${queueItem.file.name}:`,error)}))}}async processConversion(queueItem,ffmpegEntry){const{file:file,validation:validation,options:options}=queueItem;const ffmpeg=ffmpegEntry.instance;const instanceId=ffmpegEntry.id;this.currentlyProcessingFile=file.name;ffmpegEntry.currentFile=queueItem.file.name;this.showFFmpegLoadingStatus(0,queueItem.file.name,ffmpegEntry.id);try{this.activeConversions.set(file.name,{queueItemId:queueItem.id,instanceId:instanceId,controller:new AbortController});if(!options?.targetFormat){if(options){this.updateProgress(file,100,"Keeping original");return this.finalizeConversion(queueItem,file,instanceId)}else{throw new Error("Operation cancelled by user")}}const formatLabel=options.targetFormat.split("/")[1].toUpperCase();const actionType=options.targetFormat===validation.actualType?"Compressing":"Converting";this.updateProgress(file,20,`${actionType} to ${formatLabel}...`);const result=await this.convertWithFFmpeg(file,options.targetFormat,ffmpeg,queueItem);const sizeDifference=validation.originalSize-result.size;const sizeChangePercent=Math.abs(sizeDifference/validation.originalSize*100).toFixed(1);this.updateProgress(file,100,sizeDifference>0?`Size (↓) ${sizeChangePercent}%`:`Size (↑) ${sizeChangePercent}%`);return this.finalizeConversion(queueItem,result.size<validation.originalSize?result:file,instanceId)}catch(error){this.ensureCleanup(file.name);if(error.message==="Operation cancelled by user"){this.updateProgress(file,0,"Cancelled")}else{this.updateProgress(file,0,`Failed: ${error.message}`);console.error(`Conversion error for ${file.name}:`,error)}return this.finalizeConversion(queueItem,null,instanceId,error)}finally{ffmpegEntry.currentFile=null;this.currentlyProcessingFile=null}}finalizeConversion(queueItem,result,instanceId,error=null){this.ensureCleanup(queueItem.file.name);queueItem.status=error?"failed":"completed";this.activeConversions.delete(queueItem.file.name);this.currentlyProcessing--;this.releaseFFmpegInstance(instanceId);this.conversionQueue=this.conversionQueue.filter((item=>item.id!==queueItem.id));if(error){throw error}this.notifyConversionComplete(queueItem.file.name,result,error);this.conversionResults.set(queueItem.file.name,result);return result}addConversionListener(fileName,callback){if(!this.conversionListeners.has(fileName)){this.conversionListeners.set(fileName,new Set)}this.conversionListeners.get(fileName).add(callback)}removeConversionListener(fileName,callback){if(this.conversionListeners.has(fileName)){this.conversionListeners.get(fileName).delete(callback);if(this.conversionListeners.get(fileName).size===0){this.conversionListeners.delete(fileName)}}}notifyConversionComplete(fileName,result,error=null){if(error){this.ensureCleanup(fileName)}if(this.conversionListeners.has(fileName)){for(const callback of this.conversionListeners.get(fileName)){callback(result,error)}}}async convertWithFFmpeg(file,targetFormat,ffmpeg,queueItem){const fileData=queueItem.validation?.fileData||new Uint8Array(await file.arrayBuffer());const inputFileName=`input_${Date.now()}_${queueItem.id}.${file.name.split(".").pop()}`;const outputFile=`output_${Date.now()}_${queueItem.id}.${targetFormat.split("/")[1]}`;try{this.updateProgress(file,25,"Preparing audio...");console.log(`Writing ${fileData.length} bytes to ${inputFileName}`);await ffmpeg.writeFile(inputFileName,fileData);this.updateProgress(file,30,"Analyzing audio...");const optimalBitrate=await this.determineAudioBitrate(ffmpeg,inputFileName,fileData,file,queueItem.validation);console.log(`Determined optimal bitrate: ${optimalBitrate}kbps`);if(targetFormat==="audio/mp3"){CONVERSION_PRESETS["audio/mp3"].bitrate=optimalBitrate}this.updateProgress(file,40,"Converting format...");const speedOptimization=["-threads","4","-preset","veryfast"];const ffmpegArgs=["-i",inputFileName,...speedOptimization,...getFFmpegArgs(targetFormat),outputFile];console.log(`Running FFmpeg with args: ${ffmpegArgs.join(" ")}`);await ffmpeg.exec(ffmpegArgs);this.updateProgress(file,80,"Finalizing...");const data=await ffmpeg.readFile(outputFile);try{await ffmpeg.deleteFile(inputFileName);await ffmpeg.deleteFile(outputFile)}catch(cleanupError){console.warn("Non-critical error during cleanup:",cleanupError)}return new File([data],`${file.name.replace(/\.[^/.]+$/,"")}.${targetFormat.split("/")[1]}`,{type:targetFormat})}catch(error){const conversionInfo=this.activeConversions.get(file.name);if(conversionInfo?.controller?.signal?.aborted){throw new Error("Operation cancelled by user")}console.error("Audio conversion failed:",error);throw error}finally{try{await ffmpeg.deleteFile(inputFileName).catch((()=>{}));await ffmpeg.deleteFile(outputFile).catch((()=>{}))}catch(e){}}}async determineAudioBitrate(ffmpeg,inputFileName,fileData,file,validation){try{const duration=await this.getAudioDurationWithFFmpeg(ffmpeg,inputFileName);if(duration){const fileSizeKB=Math.floor(fileData.length/1024);const estimatedOriginalBitrate=Math.floor(fileSizeKB*8/duration);return this.calculateOptimalBitrate(estimatedOriginalBitrate,duration,fileSizeKB)}const webAudioDuration=await this.getAudioDurationWithWebAudio(fileData,file.type);if(webAudioDuration){const fileSizeKB=Math.floor(fileData.length/1024);const estimatedOriginalBitrate=Math.floor(fileSizeKB*8/webAudioDuration);return this.calculateOptimalBitrate(estimatedOriginalBitrate,webAudioDuration,fileSizeKB)}return this.enhancedFallbackDurationEstimation(fileData,validation?.format||file.name.split(".").pop().toLowerCase(),validation.formatInfo)}catch(error){console.warn("Couldn't determine optimal bitrate:",error);return 128}}calculateOptimalBitrate(originalBitrate,duration,fileSizeKB){let targetBitrate;if(originalBitrate<128){targetBitrate=Math.max(96,Math.floor(originalBitrate*.9))}else if(originalBitrate<192){targetBitrate=128}else if(originalBitrate<256){targetBitrate=160}else{targetBitrate=192}if(duration>600){targetBitrate=Math.min(targetBitrate,160)}if(fileSizeKB>20*1024){targetBitrate=Math.min(targetBitrate,192)}const maxBitrate=Math.floor(originalBitrate*.7);if(maxBitrate>64&&targetBitrate>maxBitrate){targetBitrate=maxBitrate}console.log(`Duration: ${duration}s, File size: ${fileSizeKB}KB, Est. original bitrate: ~${originalBitrate}kbps, Target: ${targetBitrate}kbps`);return targetBitrate}async getAudioDurationWithFFmpeg(ffmpeg,inputFileName){try{const outputInfoFile=`duration_${Date.now()}.txt`;await ffmpeg.exec(["-i",inputFileName,"-f","null","-v","quiet","-show_entries","format=duration","-of","default=noprint_wrappers=1:nokey=1","pipe:1"]);const result=await ffmpeg.readFile("pipe:1");const durationStr=(new TextDecoder).decode(result).trim();const duration=parseFloat(durationStr);if(!isNaN(duration)&&duration>0){return duration}throw new Error("Could not parse duration from FFmpeg output")}catch(error){console.warn("FFmpeg duration detection failed:",error);return null}}async getAudioDurationWithWebAudio(fileData,mimeType){return new Promise((resolve=>{try{const fileType=mimeType.toLowerCase();if(!["audio/mp3","audio/mpeg","audio/wav","audio/ogg"].includes(fileType)){resolve(null);return}const AudioContext=window.AudioContext||window.webkitAudioContext;if(!AudioContext){resolve(null);return}const audioContext=new AudioContext;audioContext.decodeAudioData(fileData.buffer?fileData.buffer:fileData,(buffer=>{const duration=buffer.duration;resolve(duration);audioContext.close().catch((()=>{}))}),(err=>{console.warn("Web Audio decode error:",err);resolve(null);audioContext.close().catch((()=>{}))}));setTimeout((()=>{resolve(null);audioContext.close().catch((()=>{}))}),3e3)}catch(error){console.warn("Web Audio API duration detection failed:",error);resolve(null)}}))}enhancedFallbackDurationEstimation(fileData,fileExt,formatInfo={}){const fileSizeKB=Math.floor(fileData.length/1024);const mergedInfo={...formatInfo,format:formatInfo.format||fileExt,vbr:formatInfo.vbr||false,hasID3:formatInfo.hasID3||false};let estimatedBitrate;switch(mergedInfo.format){case"mp3":const hasID3=mergedInfo.hasID3;const tagSizeAdjustment=hasID3?128*1024:0;if(fileSizeKB<1e3){estimatedBitrate=96}else if(fileSizeKB<5e3){estimatedBitrate=128}else{estimatedBitrate=192}if(mergedInfo.vbr){estimatedBitrate=Math.max(128,estimatedBitrate*.8)}return Math.min(estimatedBitrate,192);case"wav":return 160;case"flac":return 192;case"ogg":return 128;case"aac":case"m4a":return 160;default:if(fileSizeKB<1e3){return 96}else if(fileSizeKB<5e3){return 128}else if(fileSizeKB<15e3){return 160}else{return 192}}}async cancelConversion(fileName){console.log(`Attempting to cancel conversion for ${fileName}`);const conversionInfo=this.activeConversions.get(fileName);const wasActiveConversion=Boolean(conversionInfo);this.currentlyProcessing=Math.max(0,this.currentlyProcessing-1);if(!conversionInfo){console.log(`No active conversion found for ${fileName}`);const queuedItem=this.conversionQueue.find((item=>item.file.name===fileName&&(item.status==="queued"||item.status==="processing")));if(queuedItem){console.log(`Marking queued item as cancelled: ${fileName}`);queuedItem.active=false;queuedItem.status="cancelled";this.conversionQueue=this.conversionQueue.filter((item=>!(item.file.name===fileName&&item.status==="cancelled")))}this.updateProgress({name:fileName},0,"Cancelled");return}const{queueItemId:queueItemId,instanceId:instanceId,controller:controller}=conversionInfo;try{if(controller){controller.abort();console.log(`Abort signal sent for ${fileName}`)}if(instanceId){const instanceIndex=this.ffmpegInstances.findIndex((e=>e.id===instanceId));if(instanceIndex!==-1){const instanceEntry=this.ffmpegInstances[instanceIndex];console.log(`Terminating and reinitializing FFmpeg instance ${instanceId}`);try{await instanceEntry.instance.terminate();console.log(`FFmpeg instance ${instanceId} terminated`);const{FFmpeg:FFmpeg}=window.FFmpegWASM;const newInstance=new FFmpeg;newInstance.on("progress",(({progress:progress})=>{console.log(`FFmpeg ${instanceId} progress: ${Math.floor(progress*100)}%`);const currentFileBeingProcessed=this.ffmpegInstances.find((e=>e.id===instanceId))?.currentFile;this.showFFmpegLoadingStatus(progress,currentFileBeingProcessed,instanceId)}));newInstance.on("log",(({message:message})=>{console.log(`FFmpeg ${instanceId} log: ${message}`)}));await newInstance.load({coreURL:getFFmpegPath(FFMPEG_BASE,"ffmpeg-core.js",FFMPEG_C_V),wasmURL:getFFmpegPath(FFMPEG_BASE,"ffmpeg-core.wasm",FFMPEG_C_V)});console.log(`FFmpeg instance ${instanceId} successfully reinitialized`);this.ffmpegInstances[instanceIndex]={id:instanceId,instance:newInstance,busy:false,currentFile:null}}catch(error){console.error(`Failed to replace FFmpeg instance ${instanceId}:`,error);this.ffmpegInstances[instanceIndex].failed=true;this.ffmpegInstances[instanceIndex].busy=true}}}}catch(error){console.error(`Error during cancellation of ${fileName}:`,error)}finally{this.ensureCleanup(fileName);this.activeConversions.delete(fileName);this.conversionQueue=this.conversionQueue.filter((item=>item.id!==queueItemId));this.updateProgress({name:fileName},0,"Cancelled");this.verifyFFmpegInstances();if(wasActiveConversion){this.processQueue()}}}async verifyFFmpegInstances(){console.log("Verifying FFmpeg instances...");const workingInstances=this.ffmpegInstances.filter((instance=>!instance.failed));if(workingInstances.length<this.maxParallelConversions){console.log(`Only ${workingInstances.length} working instances, reinitializing system...`);this.ffmpegInstances=[];this.hasFFmpeg=false;this.ffmpegLoading=false;this.ffmpegLoadPromise=null;await this.initFFmpegSystem()}else{console.log(`System has ${workingInstances.length} working instances, continuing...`)}}createProgressUI(file){const wrapper=document.createElement("div");wrapper.className="upload-item";wrapper.dataset.fileName=file.name;wrapper.innerHTML=`\n      <div class="file-info">\n        <span class="filename">${file.name}</span>\n        <span class="filesize">${formatFileSize(file.size)}</span>\n      </div>\n      <div class="progress-container">\n        <progress value="0" max="100"></progress>\n        <span class="status">Waiting...</span>\n        \x3c!-- FFmpeg progress will be inserted here dynamically --\x3e\n      </div>\n      <div class="conversion-info hidden"></div>\n      <button class="cancel-btn"><span>✕</span></button>\n    `;elements.uploadProgressContainer.appendChild(wrapper);const cancelBtn=wrapper.querySelector(".cancel-btn");cancelBtn.addEventListener("click",(()=>{this.cancelConversion(file.name)}));return wrapper}updateProgress(file,percent,message=""){let wrapper=this.progressBars.get(file.name);if(message==="Cancelled"){this.ensureCleanup(file.name);if(wrapper){wrapper.classList.add("cancelled");wrapper.querySelector(".status").textContent="Cancelled";wrapper.remove();this.progressBars.delete(file.name)}return}if(!wrapper){wrapper=this.createProgressUI(file);this.progressBars.set(file.name,wrapper)}const progressBar=wrapper.querySelector("progress");const statusEl=wrapper.querySelector(".status");progressBar.value=percent;statusEl.textContent=message||`${Math.round(percent)}%`;if(percent>=100){wrapper.classList.add("completed");setTimeout((()=>{wrapper.remove();this.progressBars.delete(file.name)}),3e3)}}canConvert(){return this.hasFFmpeg}isFFmpegLoading(){return this.ffmpegLoading}}const uploadManager=new UploadManager;async function processFile(file){const validation=await validateFile(file);if(!validation.valid){showStatusMessage(validation.message,"error");return}uploadManager.updateProgress(file,0,"Preparing...");let processedFile=file;if(validation.suggestCompress||validation.needsConversion){const options=await showConversionDialog(file,validation);if(!options){uploadManager.ensureCleanup(file.name);return}if(!options.targetFormat){uploadManager.updateProgress(file,100,"Keeping original");await uploadToDB(file,file);return}try{processedFile=await uploadManager.queueAndWaitForConversion(file,validation,options);console.log(`Processed ${processedFile.name}: \n      Before: ${formatFileSize(file.size)} \n      After: ${formatFileSize(processedFile.size)}\n      Reduction: ${Math.round((1-processedFile.size/file.size)*100)}%`)}catch(error){console.error("Processing error:",error);return}}try{await uploadToDB(processedFile,file)}catch(error){if(error.message==="Operation cancelled by user"){uploadManager.updateProgress(file,0,"Cancelled")}else{uploadManager.updateProgress(file,0,"Upload failed");throw error}}}async function validateFile(file){try{if(!ALLOWED_AUDIO_TYPES.includes(file.type)){console.log(`MIME check failed: ${file.type}`);return{valid:false,message:`${file.name}: Unsupported format`}}const fileData=new Uint8Array(await file.arrayBuffer());const{type:actualType,vbr:vbr,hasID3:hasID3}=await getActualFileType(fileData);if(!actualType||!ALLOWED_AUDIO_TYPES.includes(actualType)){console.log(`Header check failed. Allowed: ${ALLOWED_AUDIO_TYPES.join(", ")}`);return{valid:false,message:`${file.name}: Actual format not allowed`}}const maxSize=FILE_SIZE_LIMITS[actualType]||FILE_SIZE_LIMITS.default;const isOverMax=file.size>maxSize;const isOverThreshold=file.size>COMPRESSION_THRESHOLD;const needsConversion=["audio/wav","audio/flac","video/mp4","audio/mp4"].includes(actualType);const suggestCompress=isOverThreshold&&!needsConversion;return{valid:!isOverMax,message:isOverMax?`${file.name} exceeds limit (${formatFileSize(maxSize)})`:`${file.name} validated (${formatFileSize(file.size)})`,actualType:actualType,suggestCompress:suggestCompress,needsConversion:needsConversion,originalSize:file.size,fileData:suggestCompress||needsConversion?fileData:null,formatInfo:{format:actualType.split("/")[1],vbr:vbr,hasID3:hasID3}}}catch(error){console.error("Validation error:",error);return{valid:false,message:`${file.name}: Validation failed - ${error.message}`}}}async function getActualFileType(fileData){try{const result={type:null,vbr:false,hasID3:false};const fullBuffer=fileData.buffer;const headerView=new DataView(fullBuffer,0,Math.min(fullBuffer.byteLength,262144));const id3Header=new Uint8Array(fullBuffer,0,10);result.hasID3=id3Header[0]===73&&id3Header[1]===68&&id3Header[2]===51;if(checkBytes(headerView,4,AUDIO_SIGNATURES.mp4.bytes)){const majorBrand=String.fromCharCode(headerView.getUint8(8),headerView.getUint8(9),headerView.getUint8(10),headerView.getUint8(11));if(AUDIO_SIGNATURES.mp4.subtypes.audio.includes(majorBrand)){result.type="audio/mp4"}else if(AUDIO_SIGNATURES.mp4.subtypes.video.includes(majorBrand)){result.type="video/mp4"}else{const mdatCheck=new Uint8Array(fullBuffer,20,4);result.type=mdatCheck[0]===109&&mdatCheck[1]===100&&mdatCheck[2]===97&&mdatCheck[3]===116?"audio/mp4":"video/mp4"}}for(const[format,{offset:offset,bytes:bytes}]of Object.entries(AUDIO_SIGNATURES)){if(checkBytes(headerView,offset,bytes)){result.type=`audio/${format}`;break}}if(!result.type&&(headerView.getUint8(0)===255&&(headerView.getUint8(1)&224)===224)){result.type="audio/mpeg";const vbrCheckBuffer=new Uint8Array(fullBuffer,0,Math.min(fullBuffer.byteLength,1048576));const vbrText=(new TextDecoder).decode(vbrCheckBuffer);result.vbr=vbrText.includes("Xing")||vbrText.includes("VBRI")}if(fullBuffer.byteLength>=128){const footerStart=fullBuffer.byteLength-128;const id3v1Footer=new Uint8Array(fullBuffer,footerStart,3);result.hasID3=result.hasID3||id3v1Footer[0]===84&&id3v1Footer[1]===65&&id3v1Footer[2]===71}if(!result.type&&fileData.fileName){const ext=fileData.fileName.split(".").pop().toLowerCase();if(AUDIO_EXTENSIONS.includes(ext)){result.type=`audio/${ext}`}}return result}catch(error){console.error("File type detection failed:",error);return{type:null,vbr:false,hasID3:false}}}function checkBytes(view,offset,bytes){return bytes.every(((b,i)=>view.getUint8(offset+i)===b))}async function showConversionDialog(file,validation){return new Promise((resolve=>{const dialog=document.createElement("div");dialog.className="conversion-dialog";const options=[];if(validation.needsConversion){options.push(...(CONVERSION_OPTIONS[validation.actualType]||CONVERSION_OPTIONS.default).map((format=>({label:`Convert to ${format.split("/")[1].toUpperCase()}`,action:()=>resolve({targetFormat:format})}))),{label:"Keep original size",action:()=>resolve({})})}else if(validation.suggestCompress){options.push({label:`Compress to smaller file size`,action:()=>resolve({targetFormat:validation.actualType})},{label:"Keep original size",action:()=>resolve({})})}options.push({label:"Cancel upload",action:()=>resolve(null)});dialog.innerHTML=`\n      <div class="dialog-content">\n        <h3>Process ${file.name}?</h3>\n        <p>${validation.needsConversion?"File format not supported directly.":`File is quite large (${formatFileSize(file.size)})`}</p>\n        <div class="countdown-timer">Auto-selecting in <span class="timer">30</span>s</div>\n        <div class="options">\n          ${options.map((opt=>`<button class="option-btn">${opt.label}</button>`)).join("")}\n        </div>\n      </div>\n    `;document.body.appendChild(dialog);options.forEach(((opt,i)=>{dialog.querySelectorAll(".option-btn")[i].addEventListener("click",(()=>{opt.action();dialog.remove()}))}));const timerEl=dialog.querySelector(".timer");let timeLeft=30;const updateTimer=()=>{timerEl.textContent=timeLeft;timeLeft--};const intervalId=setInterval(updateTimer,1e3);const timeoutId=setTimeout((()=>{clearInterval(intervalId);if(options.length>0){options[0].action()}else{resolve(null)}dialog.remove()}),3e4)}))}function getFFmpegArgs(format){const preset=CONVERSION_PRESETS[format];switch(format){case"audio/mp3":return["-codec:a","libmp3lame","-b:a",`${preset.bitrate}k`,"-joint_stereo","1","-q:a","5","-compression_level","9","-cutoff","18000","-f","mp3"];case"audio/ogg":return["-codec:a","libvorbis","-qscale:a",preset.quality.toString(),"-compression_level","10","-f","ogg"];default:return["-f",format.split("/")[1]]}}async function uploadToDB(file,originalFile=null){const isProcessed=originalFile!==null&&file!==originalFile;const updateProgress=percent=>uploadManager.updateProgress(file,50+percent/2,percent<100?"Uploading...":"Upload complete");updateProgress(0);try{const chunkSize=2*1024*1024;let fileData;if(file.size>chunkSize){const totalChunks=Math.ceil(file.size/chunkSize);const chunks=[];for(let i=0;i<totalChunks;i++){const start=i*chunkSize;const end=Math.min(start+chunkSize,file.size);const chunk=file.slice(start,end);const chunkData=await readFileAsArrayBuffer(chunk);chunks.push(chunkData);updateProgress(i/totalChunks*50)}updateProgress(50,"Preparing data...");fileData=await combineArrayBuffers(chunks)}else{fileData=await readFileAsArrayBuffer(file);updateProgress(40)}updateProgress(75,"Saving to database...");const db=await getDB();await new Promise(((resolve,reject)=>{const tx=db.transaction("items","readwrite");tx.onabort=()=>reject(new Error("Transaction was aborted"));tx.onerror=()=>reject(tx.error||new Error("Transaction failed"));const store=tx.objectStore("items");const request=store.add({name:file.name.replace(/\.[^/.]+$/,""),data:fileData,type:"user",timestamp:Date.now(),processed:isProcessed,originalSize:originalFile?originalFile.size:file.size,finalSize:file.size});request.onsuccess=()=>{updateProgress(100);resolve()};request.onerror=()=>reject(request.error)}))}catch(error){console.error("Upload error:",error);throw error}}function readFileAsArrayBuffer(file){return new Promise(((resolve,reject)=>{const reader=new FileReader;reader.onload=()=>resolve(reader.result);reader.onerror=()=>reject(reader.error);reader.readAsArrayBuffer(file)}))}function combineArrayBuffers(buffers){const totalLength=buffers.reduce(((sum,buffer)=>sum+buffer.byteLength),0);const result=new Uint8Array(totalLength);let offset=0;for(const buffer of buffers){result.set(new Uint8Array(buffer),offset);offset+=buffer.byteLength}return result.buffer}function formatFileSize(bytes){const sizes=["Bytes","KB","MB","GB"];if(bytes===0)return"0 Bytes";const i=Math.floor(Math.log(bytes)/Math.log(1024));return parseFloat((bytes/Math.pow(1024,i)).toFixed(2))+" "+sizes[i]}function togglePlaylistVisibility(){state.isPlaylistVisible=!state.isPlaylistVisible;elements.playlistSection.classList.toggle("visible",state.isPlaylistVisible);if(window.matchMedia("(max-width: 767px) and (orientation: portrait)").matches){elements.playlistSection.style.display="block";void elements.playlistSection.offsetHeight}saveState();const message=state.isPlaylistVisible?"Playlist shown":"Playlist hidden";showStatusMessage(message);if(state.speechEnabled)speakText(message)}function toggleSelection(index){if(state.selectedItems.has(index)){state.selectedItems.delete(index)}else{state.selectedItems.add(index)}renderPlayList()}function playItem(index){clearSelections();loadItem(index);state.selectedItems.add(index);renderPlayList()}function clearSelections(){state.selectedItems.clear();renderPlayList()}function handleSelectAll(){state.selectedItems=new Set([...Array(state.items.length).keys()]);renderPlayList()}async function handleDeleteSelected(){if(state.selectedItems.size===0){showStatusMessage("No items selected to delete","info");return}try{const database=await getDB();const tx=database.transaction("items","readwrite");const store=tx.objectStore("items");const indicesToDelete=Array.from(state.selectedItems);const itemsToDelete=indicesToDelete.map((index=>state.items[index]));const deletingDefaults=itemsToDelete.some((item=>item.type==="default"));if(deletingDefaults){state.defaultsDeleted=true;saveState()}let deletedCount=0;for(const item of itemsToDelete){const req=store.delete(item.id);req.onsuccess=()=>deletedCount++}await new Promise(((resolve,reject)=>{tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(new Error("Transaction aborted"))}));state.selectedItems.clear();localStorage.setItem("defaultsDeleted","true");if(indicesToDelete.length===state.items.length){state.items=[];renderPlayList();state.audio.pause();state.audio.src="";elements.playPauseBtn.textContent="▶";showStatusMessage("All items deleted. Playlist empty.","info")}else{await loadItems(false);showStatusMessage(`${deletedCount} item(s) deleted`,"success")}}catch(error){console.error("Delete failed:",error);showStatusMessage("Error deleting items","error")}}export{loadItems,uploadManager,handleFileUpload,togglePlaylistVisibility,toggleSelection,playItem,clearSelections,handleSelectAll,handleDeleteSelected};